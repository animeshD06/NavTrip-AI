[build]
  command = "cd frontend && flutter build web --release --dart-define=API_BASE_URL=$API_BASE_URL"
  publish = "frontend/build/web"

[build.environment]
  FLUTTER_VERSION = "stable"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
